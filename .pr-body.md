Writes down what mark-brannan/.github#18 ruled: nothing calling into this repository is ever pinned, at any hop, permanently. Reverts the second, inner pin nobody caught in the first pass — `prose-budget.yml`'s own `dotfiles-ref` default still pointed at an immutable `mark-brannan/dotfiles` tag after the outer `@v1` consumer pin was reverted.

- `dotfiles-ref` input default: `prose-budget/v1.0.0` → `main`
- README table + rationale paragraph rewritten to state the policy plainly and link the reverting PRs
- `prose-budget.yml`'s own header comment (the calling snippet other repos copy) updated to `@main`

Related: mark-brannan/colregs#78, mark-brannan/colregs-engine#54, mark-brannan/symphony#66 (the consumer-side reverts), mark-brannan/.github#18 (closed, the ruling this records).

🤖 Generated with [Claude Code](https://claude.com/claude-code)
