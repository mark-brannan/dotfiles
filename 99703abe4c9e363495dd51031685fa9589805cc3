review PRRT_kwDOAsco8c6lCDRY: state_lock no longer installs a trap (it overwrote a caller's, e.g. stop-continuity.sh's restore_board); callers add state_unlock to their own trap instead. Added a test for a caller's pre-existing EXIT trap surviving state_lock.
